/*Implements pca on X with dims n x d to reduce to n x initial_dims*/
void pca(int n, int *d, double X[n][*d], int initial_dims){
	*d=initial_dims;
	printf("n: %d\n", n);
	printf("d: %d\n", *d);

	// Take away mean

	// Get eigenvalues and eigenvectors

	// Return vector of size initial_dims of top eigenvectors

}